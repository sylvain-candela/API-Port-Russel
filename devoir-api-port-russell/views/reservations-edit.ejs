<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/dashboard.css">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/catways.css">
    <link rel="stylesheet" href="/catways-add.css">
    <link rel="stylesheet" href="/login.css">
    <title>Modifier la Réservation</title>
</head>
<body>
    <nav>
        <div class="container">
            <h2>Menu</h2>
            <ul class="menu">
                <li><a href="/reservations">page précédente</a></li>
                <li><a href="/dashboard">Tableau de bord</a></li>
                <li><a href="/catways">Gestion des Catways</a></li>
                <li><a href="/users/logout">Se déconnecter</a></li>
            </ul>
        </div>

        <header class="container">
            <h1>Modifier la réservation</h1>
            <p>Catway concerné : <strong><%= reservation.catwayNumber %></strong></p>
        </header>

        <section class="container">
            <form action="/reservations/<%= reservation._id %>?_method=PUT" method="POST" class="edit-form">
                
                <div class="form-group">
                    <label for="clientName">Nom du Client :</label>
                    <input type="text" name="clientName" id="clientName" value="<%= reservation.clientName %>" required>
                </div>

                <div class="form-group">
                    <label for="boatName">Nom du Bateau :</label>
                    <input type="text" name="boatName" id="boatName" value="<%= reservation.boatName %>" required>
                </div>

                <div class="form-group">
                    <label for="startDate">Date de début :</label>
                    <input type="date" name="startDate" id="startDate" 
                           value="<%= new Date(reservation.startDate).toISOString().split('T')[0] %>" required>
                </div>

                <div class="form-group">
                    <label for="endDate">Date de fin :</label>
                    <input type="date" name="endDate" id="endDate" 
                           value="<%= new Date(reservation.endDate).toISOString().split('T')[0] %>" required>
                </div>

                <div class="buttons" style="margin-top: 20px;">
                    <button type="submit" class="button">Enregistrer les modifications</button>
                    <a href="/reservations" class="button-cancel">Annuler</a>
                </div>
            </form>
        </section>
    </nav>
</body>
</html>